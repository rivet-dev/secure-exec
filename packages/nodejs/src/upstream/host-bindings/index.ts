/**
 * Host-side upstream binding providers land here in later stories.
 *
 * The scaffold keeps this directory explicit so bootstrap-critical bindings can
 * stay separate from the current bridge-first runtime while the replacement
 * path is still internal-only.
 */
export const upstreamHostBindingModules: readonly string[] = Object.freeze([]);
