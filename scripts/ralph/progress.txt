# Ralph Progress Log
Started: Mon Mar 30 08:41:55 PM PDT 2026
---

## Codebase Patterns
- Active module-load benchmark artifacts live in `packages/secure-exec/benchmarks/results/module-load/`; refresh them with the full `pnpm --filter secure-exec bench:module-load` run after each performance story.
- Headless CLI workloads inside standalone `NodeRuntime.exec()` should pass `stdin: ""`; Pi's direct `dist/main.js` / `dist/cli.js` path still exits before provider traffic there, so the current trustworthy Pi CLI benchmark imports the CLI module graph and then drives `runPrintMode()` through the SDK.
- For Pi startup and end-to-end perf stories, record warm wall time together with bridge calls/iteration and dominant IPC counters (`_loadPolyfill`, BridgeResponse bytes, and any new network callsites) instead of relying on a single aggregate number.

## [2026-03-30 21:00 PDT] - US-001
- Implemented a real Pi CLI end-to-end benchmark path by importing the CLI module graph, explicitly closing headless stdin, and then running Pi print-mode through the SDK against the mock Anthropic server. Also fixed standalone `NodeRuntime` empty-stdin EOF semantics and added focused regression coverage.
- Benchmark deltas:
  - Pi CLI end-to-end: failed baseline (`code = 0`, `mockRequests = 0`, empty stdout/stderr) -> passed workload; warm wall `n/a` -> `1828.258 ms`; `mockRequests` `0` -> `1` per iteration; bridge calls `n/a` -> `5797`/iteration; `_loadPolyfill` `n/a` -> `5716` calls/iteration (`~884.473 ms` host bridge time/iteration); `BridgeResponse` `n/a` -> `9478051` bytes/iteration; `Execute` `n/a` -> `1242782` encoded bytes/iteration.
  - Pi CLI startup control: warm wall `1810.580 ms` -> `1721.305 ms`; bridge calls stayed `5336`/iteration; `_loadPolyfill` stayed `5247` calls/iteration (`~801.958 ms` host bridge time/iteration); `BridgeResponse` is now `9130224` bytes/iteration.
- Added a follow-up blocker story and todo entry for restoring the direct standalone Pi CLI headless path (`dist/main.js` / `dist/cli.js`) so the benchmark can eventually drop the current surrogate.
- Files changed: `AGENTS.md`, `.agent/contracts/node-runtime.md`, `packages/nodejs/src/bridge/process.ts`, `packages/secure-exec/tests/runtime-driver/node/runtime.test.ts`, `packages/secure-exec/benchmarks/module-load/scenario-catalog.ts`, `packages/secure-exec/benchmarks/module-load/scenario-runner.ts`, `packages/secure-exec/benchmarks/results/module-load/*`, `docs-internal/todo.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Standalone `NodeRuntime` buffered stdin must emit EOF even for empty-string input; otherwise headless CLI code can await `process.stdin` forever and still exit `0`.
  - Pi CLI's direct standalone headless path does not currently honor the benchmark's mock-provider setup; importing the CLI module graph and then driving SDK `runPrintMode()` is the current trustworthy benchmark workaround.
  - For Pi CLI benchmark sanity checks, `mockRequests`, `_loadPolyfill` per iteration, and BridgeResponse bytes reveal quickly whether the scenario reached a real workload.
---
