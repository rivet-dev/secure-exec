# Ralph Progress Log
Started: Wed Mar 18 06:52:02 PM PDT 2026
---

## Codebase Patterns
- Use `pnpm turbo run check-types` for TypeScript typecheck (not `typecheck`)
- Rust crate lives at `crates/v8-runtime/`; binary name is `secure-exec-v8`
- `cargo check` in `crates/v8-runtime/` validates Rust compilation
- rusty_v8 crate is imported as `v8` (e.g., `v8 = "130"` in Cargo.toml)
- IPC enums use `#[serde(tag = "type")]` (internally tagged) for JS interop
- Use `serde_bytes` for `Vec<u8>` fields to get MessagePack bin format
- Use `rmp_serde::to_vec_named` (not `to_vec`) for named-field serialization
- Framing functions: `write_message(&mut writer, &msg)` and `read_message(&mut reader)` in `ipc.rs`
- Max IPC message size is 64MB (`MAX_MESSAGE_SIZE` constant in `ipc.rs`)
- V8 tests MUST run with `--test-threads=1` to avoid SIGSEGV from parallel V8 operations
- `signal-hook::flag::register` clears the AtomicBool to `false` on signal receipt
- `v8::Context::new(scope, Default::default())` — v130 requires a `ContextOptions` second arg
- Use `v8::Global<v8::Context>` to persist context handles across HandleScope lifetimes
- Consolidate V8 tests into a single `#[test]` fn to avoid inter-test isolate state issues
- V8 platform MUST be initialized on the main thread before spawning session threads (SIGSEGV otherwise)
- Session tests use `#[cfg(not(test))]` to skip V8 isolate creation; V8 lifecycle tested in `execution::tests`
- ALL V8 tests live in `execution::tests::v8_inject_globals_and_hardening` — do NOT add new `#[test]` fns that create V8 isolates
- Use `obj.set_integrity_level(scope, v8::IntegrityLevel::Frozen)` for Object.freeze()
- Use `obj.define_own_property(scope, key, val, READ_ONLY | DONT_DELETE)` for non-writable, non-configurable properties
- Use `isolate.set_allow_wasm_code_generation_callback(fn)` to disable WASM — callback returns false
- Use `scope.get_current_context()` to get the context from within a ContextScope
- `session_id` is `String` in IPC types (not u32/u128) for JS interop; use hex nonces for 128-bit security
- Session concurrency is configurable via `SECURE_EXEC_V8_MAX_SESSIONS` env var (default: available CPUs)
- Use `std::thread::available_parallelism()` instead of `num_cpus` crate for CPU count
---

## 2026-03-18 - US-001
- Scaffolded `crates/v8-runtime/` with Cargo.toml and all module stubs
- Files created: Cargo.toml, src/{main,ipc,isolate,execution,bridge,host_call,timeout,stream}.rs
- Dependencies: v8 (130), rmp-serde (1), serde (1, derive), crossbeam-channel (0.5)
- **Learnings for future iterations:**
  - The turbo task for typecheck is `check-types`, not `typecheck`
  - `pnpm install` may be needed before first turbo run if node_modules is missing
  - v8 crate compiles quickly with prebuilt V8 static lib (~8s for check)
  - main.rs declares all modules with `mod` statements; each .rs file is a module
---

## 2026-03-18 - US-002
- Defined all IPC message types in `ipc.rs`: HostMessage (7 variants) and RustMessage (4 variants)
- Added ExecutionError structured type (type, message, stack, optional code)
- Added helper types: ExecuteMode, LogChannel, ProcessConfig, OsConfig
- Added serde_bytes dependency for efficient binary field serialization
- 22 unit tests verifying MessagePack round-trip for every message variant
- Files changed: `crates/v8-runtime/src/ipc.rs`, `crates/v8-runtime/Cargo.toml`
- **Learnings for future iterations:**
  - `#[serde(tag = "type")]` works with rmp-serde for internally tagged enums
  - `serde_bytes` works on `Option<Vec<u8>>` with `#[serde(with = "serde_bytes")]`
  - `rmp_serde::to_vec_named` is needed for named-field maps (not `to_vec` which uses positional)
  - `#[serde(skip_serializing_if = "Option::is_none")]` correctly omits keys from msgpack maps
  - Spec has `session_id: u32` in Message Types section but `u128` in Resource Budgets — used u32 for now, US-007 will upgrade
---

## 2026-03-18 - US-003
- Implemented `write_message()` and `read_message()` generic functions in `ipc.rs`
- Format: [4-byte u32 big-endian length prefix][N-byte MessagePack payload]
- 64MB max message size enforced on both read and write
- Added 10 unit tests: framing round-trip for HostMessage/RustMessage, BE length prefix verification, multiple sequential messages, oversized read rejection (64MB+1, u32::MAX), exact-64MB acceptance, binary data round-trip, EOF handling
- Files changed: `crates/v8-runtime/src/ipc.rs`
- **Learnings for future iterations:**
  - `std::io::Cursor` is useful for testing Read/Write trait functions against in-memory buffers
  - `read_exact` returns `UnexpectedEof` when not enough data, distinct from `InvalidData` for size limits
  - `write_message` serializes then checks size; for truly oversized payloads, serialization happens first (memory implication for future hardening)
---

## 2026-03-18 - US-004
- Implemented V8 platform init (`init_v8_platform()` with `Once`) and isolate lifecycle (`create_isolate()`, `create_context()`) in `isolate.rs`
- Platform initialization is safe to call multiple times (idempotent via `std::sync::Once`)
- `create_isolate()` supports optional `heap_limit_mb` parameter
- `create_context()` returns `v8::Global<v8::Context>` for cross-scope persistence
- Single consolidated test verifying: 3 sequential create/destroy cycles, heap-limited isolate, unlimited isolate, and Global context handle state persistence
- Files changed: `crates/v8-runtime/src/isolate.rs`
- **Learnings for future iterations:**
  - V8 tests crash (SIGSEGV) when multiple `#[test]` functions create/destroy isolates even with `--test-threads=1` — consolidate V8 tests into single test functions
  - rusty_v8 v130 changed `Context::new` to require a `ContextOptions` second arg (use `Default::default()`)
  - `v8::Global::new(scope, local)` creates a persistent handle; when dropped, it resets via the stored `IsolateHandle`
  - `v8::CreateParams::default().heap_limits(0, limit_bytes)` sets the max heap size
---

## 2026-03-18 - US-005
- Implemented UDS listener in `main.rs` with socket path security
- Creates tmpdir with 128-bit random hex suffix under system temp dir
- Sets directory permissions to 0700 before binding socket
- Binds `UnixListener` to `secure-exec.sock` inside the tmpdir
- Prints socket path to stdout (with explicit flush for pipe consumers)
- Registers SIGTERM and SIGINT handlers via `signal-hook` for graceful shutdown
- Non-blocking accept loop polls shutdown flag every 10ms
- Cleans up socket file and directory on shutdown
- Files changed: `crates/v8-runtime/src/main.rs`, `crates/v8-runtime/Cargo.toml`
- **Learnings for future iterations:**
  - `signal-hook::flag::register` sets an AtomicBool to `false` on signal (inverts the running flag pattern) — pass the Arc directly, it clears to false
  - Rust's `UnixListener::bind()` combines bind() and listen() — can't separate them without raw libc
  - Setting 0700 on the directory before binding is equivalent to after-bind/before-listen when the directory name has 128-bit randomness
  - Must flush stdout explicitly after printing socket path — Rust stdout is fully-buffered when piped
  - `signal-hook` v0.3 was pulled (v0.4 available but 0.3 is fine for AtomicBool flag pattern)
---

## 2026-03-18 - US-007
- Implemented session management with V8 isolate threads, connection binding, and concurrency limiting
- Changed `session_id` from `u32` to `String` across all IPC message types for 128-bit nonce support
- Created `session.rs` with `SessionManager`: create/destroy sessions, connection ownership enforcement, max concurrency with Condvar-based queuing
- Updated `main.rs`: added connection handler threads (per authenticated connection), session message dispatch, graceful shutdown of all sessions, V8 platform init on main thread
- Max concurrency configurable via `SECURE_EXEC_V8_MAX_SESSIONS` env var (default: `available_parallelism()`)
- Files changed: `crates/v8-runtime/src/ipc.rs`, `crates/v8-runtime/src/main.rs`, `crates/v8-runtime/src/session.rs` (new)
- **Learnings for future iterations:**
  - V8 platform MUST be initialized on the main thread BEFORE spawning any threads that create isolates — otherwise SIGSEGV
  - Session tests cannot create V8 isolates due to inter-test V8 state issues; use `#[cfg(not(test))]` to skip V8 in tests, test V8 lifecycle separately in `isolate::tests`
  - `String` is the best type for session_id in IPC: JS interop is clean (hex nonce), works as HashMap key, avoids u128/MessagePack incompatibility
  - `std::thread::available_parallelism()` (stable since Rust 1.59) replaces the `num_cpus` crate
  - `BridgeResponse` messages don't carry session_id; routing will need a separate mechanism (call_id → session mapping) in later stories
  - Connection handler threads handle the message loop; the main thread only does accept + auth
---

## 2026-03-18 - US-008
- Implemented InjectGlobals handling and V8 context hardening in `execution.rs`
- `inject_globals()`: sets `_processConfig` and `_osConfig` as frozen, non-writable, non-configurable global properties
- `disable_wasm()`: disables WASM compilation via `set_allow_wasm_code_generation_callback` returning false
- SharedArrayBuffer removed from global when `timing_mitigation` is "freeze", preserved otherwise
- Env sub-object within `_processConfig` is also frozen
- Updated `session.rs` to handle InjectGlobals messages and call `disable_wasm` on isolate creation
- Consolidated all V8 tests into single `execution::tests::v8_inject_globals_and_hardening` test (moved isolate lifecycle tests from `isolate.rs`)
- 7-part test covering: config values, null frozen_time_ms, frozen/non-writable/non-configurable properties, SharedArrayBuffer removal in freeze mode, SharedArrayBuffer preserved in non-freeze mode, WASM disabled, WASM enabled by default
- Files changed: `crates/v8-runtime/src/execution.rs`, `crates/v8-runtime/src/session.rs`, `crates/v8-runtime/src/isolate.rs`
- **Learnings for future iterations:**
  - `v8::PropertyAttribute` does NOT implement `Copy` — must create a new instance for each `define_own_property` call
  - Multiple `#[test]` functions that create V8 isolates cause SIGSEGV even with `--test-threads=1` — all V8 tests MUST live in a single test function
  - `scope.get_current_context()` works within a ContextScope to retrieve the current context
  - `Object.freeze()` is `set_integrity_level(scope, IntegrityLevel::Frozen)` — only shallow-freezes, so nested objects (like env) need separate freeze calls
  - `obj.delete(scope, key.into())` removes a property from global (used for SharedArrayBuffer removal)
  - `deny_wasm_code_generation` callback must be `extern "C"` with signature `fn(Local<Context>, Local<String>) -> bool`
---
